# vim:set ft=sh:
install ()
{
    MODULES="aufs"
    BINARIES="/bin/bash /bin/date /sbin/fsck.jfs /usr/bin/pstree"
    FILES="/etc/rc.conf"
    SCRIPT="ppz_tmp"
}

help ()
{
cat<<HELPEOF
   This is my nifty universal fs-logic crap :D
HELPEOF
}
