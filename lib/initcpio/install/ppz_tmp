# vim:set ft=sh:
install () {
    MODULES="dm-crypt btrfs fuse"
    BINARIES="/bin/bash /bin/date /sbin/fsck.jfs /usr/bin/pstree /bin/cat /usr/bin/head"
    FILES="/etc/rc.conf /etc/rc.conf.basics"
    SCRIPT="ppz_tmp"
}

help () {
	 cat <<-EOF
		This is my nifty universal fs-logic crap :D
		EOF
}
