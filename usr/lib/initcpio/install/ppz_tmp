# vim:set ft=sh:
build () {
    MODULES="btrfs dm-crypt dm-mod dm-snapshot fuse"
    BINARIES="/bin/bash /bin/date /usr/bin/pstree /bin/cat /usr/bin/head"
    FILES="/etc/rc.conf"
    SCRIPT="ppz_tmp"
}

help () {
	 cat <<-EOF
		This is my nifty universal fs-logic crap :D
		EOF
}
