#!/usr/bin/bash

. /etc/ppz.conf
. /usr/share/ppz/bash.functions

am_root || exit

while :; do
    test_network -n0 -t10 -w15
    stamp "Network down! restarting..."
    systemctl restart "${NET_SERVICES[@]}"
done
