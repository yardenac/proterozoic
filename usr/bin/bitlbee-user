#!/bin/bash

# run this command to make sure bitlbee is running as user
# can run many times, should be very efficient

mkdir -p ~/.config/bitlbee
chmod 700 ~/.config/bitlbee
one-instance -s -u /usr/sbin/bitlbee -F \
	 -d ~/.config/bitlbee/

#	 -c ~/.config/bitlbee.conf \
